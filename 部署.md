A. 伺服器首次設定（SSH 進 192.168.31.42 執行一次）
ssh root@192.168.31.42
apt update && apt install -y nginx
mkdir -p /var/www/dicj
ufw allow 80
B. 上傳 nginx 設定並啟用
在你的 Mac 執行：
cd "/Users/souben/Library/Mobile Documents/com~apple~CloudDocs/Meta/DICJ 娛樂場禁入申請系統/html"
scp nginx-dicj.conf root@192.168.31.42:/etc/nginx/sites-available/dicj.conf
回到伺服器執行：
ln -s /etc/nginx/sites-available/dicj.conf /etc/nginx/sites-enabled/
rm -f /etc/nginx/sites-enabled/default
nginx -t && systemctl reload nginx
C. 上傳網站檔案（每次更新都執行一次）
在你的 Mac 執行：
cd "/Users/souben/Library/Mobile Documents/com~apple~CloudDocs/Meta/DICJ 娛樂場禁入申請系統/html"
chmod +x deploy-nginx.sh
./deploy-nginx.sh
